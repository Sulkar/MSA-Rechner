import { reactive } from "vue";

export const store = reactive({
  /*Mathematik: { Zeugnis: 0, Prüfung: 0, Gesamt: 0 },
  Religion: { Zeugnis: 0, Prüfung: 0, Gesamt: 0 },
  Deutsch: { Zeugnis: 0, Prüfung: 0, Gesamt: 0 },

  Gesamtnote: 0,
  setNote(fach, typ, note) {
    this[fach][typ] = note;
    let sumMathematik = 0;
    Object.values(this.Mathematik).forEach((note) => {
      sumMathematik += note;
    });

    this.Gesamtnote = sumMathematik;
  },*/
});
